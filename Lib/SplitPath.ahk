SplitPath(filePath)
{
	SplitPath, filePath, FileName, dir, extension, nameNoExt, drive
	return r := []
	, r.filePath := filePath
	, r.fileName := fileName
	, r.dir := dir
	, r.extension := extension
	, r.nameNoExt := nameNoExt
	, r.drive := drive
}